IF (NOT ${M_gnu_xx} MATCHES "NOTFOUND")
CHECK_MUTATEE_COMPILER ("${M_gnu_xx}"
	"${MUTATOR_DEFINES}  ${CXXFLAGS} -DSOLO_MUTATEE ${MUTATEE_CXXFLAGS_GNU}     -dynamic -Wl,-export-dynamic -ldl -ldl -lstdc++"
	"-dynamic -Wl,-export-dynamic -ldl -lstdc++"
	dummyMUTATEE_bgq_gxx64dyn)
IF (dummyMUTATEE_bgq_gxx64dyn)
SET (MUTATEE_bgq_gxx64dyn 1 CACHE STRING "Build mutatees: compiler bgq_g++, ABI 64-bit, dyn linked")
ELSE()
SET (MUTATEE_bgq_gxx64dyn 0 CACHE STRING "Skip mutatees: compilers bgq_g++, ABI 64-bit, dyn linked")
ENDIF()
ENDIF()
IF (NOT ${M_gnu_xx} MATCHES "NOTFOUND")
CHECK_MUTATEE_COMPILER ("${M_gnu_xx}"
	"${MUTATOR_DEFINES}  ${CXXFLAGS} -DSOLO_MUTATEE ${MUTATEE_CXXFLAGS_GNU}     -static -ldl -ldl -lstdc++"
	"-static -ldl -lstdc++"
	dummyMUTATEE_bgq_gxx64stat)
IF (dummyMUTATEE_bgq_gxx64stat)
SET (MUTATEE_bgq_gxx64stat 1 CACHE STRING "Build mutatees: compiler bgq_g++, ABI 64-bit, stat linked")
ELSE()
SET (MUTATEE_bgq_gxx64stat 0 CACHE STRING "Skip mutatees: compilers bgq_g++, ABI 64-bit, stat linked")
ENDIF()
ENDIF()
IF (NOT ${M_gnu_cc} MATCHES "NOTFOUND")
CHECK_MUTATEE_COMPILER ("${M_gnu_cc}"
	"${MUTATOR_DEFINES}  ${CFLAGS} -DSOLO_MUTATEE ${MUTATEE_CFLAGS_GNU}     -dynamic -Wl,-export-dynamic -ldl -ldl"
	"-dynamic -Wl,-export-dynamic -ldl"
	dummyMUTATEE_bgq_gcc64dyn)
IF (dummyMUTATEE_bgq_gcc64dyn)
SET (MUTATEE_bgq_gcc64dyn 1 CACHE STRING "Build mutatees: compiler bgq_gcc, ABI 64-bit, dyn linked")
ELSE()
SET (MUTATEE_bgq_gcc64dyn 0 CACHE STRING "Skip mutatees: compilers bgq_gcc, ABI 64-bit, dyn linked")
ENDIF()
ENDIF()
IF (NOT ${M_gnu_cc} MATCHES "NOTFOUND")
CHECK_MUTATEE_COMPILER ("${M_gnu_cc}"
	"${MUTATOR_DEFINES}  ${CFLAGS} -DSOLO_MUTATEE ${MUTATEE_CFLAGS_GNU}     -static -ldl -ldl"
	"-static -ldl"
	dummyMUTATEE_bgq_gcc64stat)
IF (dummyMUTATEE_bgq_gcc64stat)
SET (MUTATEE_bgq_gcc64stat 1 CACHE STRING "Build mutatees: compiler bgq_gcc, ABI 64-bit, stat linked")
ELSE()
SET (MUTATEE_bgq_gcc64stat 0 CACHE STRING "Skip mutatees: compilers bgq_gcc, ABI 64-bit, stat linked")
ENDIF()
ENDIF()
IF (NOT ${M_bg_cc} MATCHES "NOTFOUND")
CHECK_MUTATEE_COMPILER ("${M_bg_cc}"
	"${MUTATOR_DEFINES}  ${CFLAGS} ${CFLAGS}    -qnostaticlink -ldl -ldl"
	"-qnostaticlink -ldl"
	dummyMUTATEE_bgxlc64dyn)
IF (dummyMUTATEE_bgxlc64dyn)
SET (MUTATEE_bgxlc64dyn 1 CACHE STRING "Build mutatees: compiler bgxlc, ABI 64-bit, dyn linked")
ELSE()
SET (MUTATEE_bgxlc64dyn 0 CACHE STRING "Skip mutatees: compilers bgxlc, ABI 64-bit, dyn linked")
ENDIF()
ENDIF()
IF (NOT ${M_bg_cc} MATCHES "NOTFOUND")
CHECK_MUTATEE_COMPILER ("${M_bg_cc}"
	"${MUTATOR_DEFINES}  ${CFLAGS} ${CFLAGS}     -ldl -ldl"
	" -ldl"
	dummyMUTATEE_bgxlc64stat)
IF (dummyMUTATEE_bgxlc64stat)
SET (MUTATEE_bgxlc64stat 1 CACHE STRING "Build mutatees: compiler bgxlc, ABI 64-bit, stat linked")
ELSE()
SET (MUTATEE_bgxlc64stat 0 CACHE STRING "Skip mutatees: compilers bgxlc, ABI 64-bit, stat linked")
ENDIF()
ENDIF()
IF (NOT ${M_bg_CC} MATCHES "NOTFOUND")
CHECK_MUTATEE_COMPILER ("${M_bg_CC}"
	"${MUTATOR_DEFINES}  ${CXXFLAGS} ${CXXFLAGS}    -qnostaticlink -ldl -ldl -lstdc++"
	"-qnostaticlink -ldl -lstdc++"
	dummyMUTATEE_bgxlcxx64dyn)
IF (dummyMUTATEE_bgxlcxx64dyn)
SET (MUTATEE_bgxlcxx64dyn 1 CACHE STRING "Build mutatees: compiler bgxlc++, ABI 64-bit, dyn linked")
ELSE()
SET (MUTATEE_bgxlcxx64dyn 0 CACHE STRING "Skip mutatees: compilers bgxlc++, ABI 64-bit, dyn linked")
ENDIF()
ENDIF()
IF (NOT ${M_bg_CC} MATCHES "NOTFOUND")
CHECK_MUTATEE_COMPILER ("${M_bg_CC}"
	"${MUTATOR_DEFINES}  ${CXXFLAGS} ${CXXFLAGS}     -ldl -ldl -lstdc++"
	" -ldl -lstdc++"
	dummyMUTATEE_bgxlcxx64stat)
IF (dummyMUTATEE_bgxlcxx64stat)
SET (MUTATEE_bgxlcxx64stat 1 CACHE STRING "Build mutatees: compiler bgxlc++, ABI 64-bit, stat linked")
ELSE()
SET (MUTATEE_bgxlcxx64stat 0 CACHE STRING "Skip mutatees: compilers bgxlc++, ABI 64-bit, stat linked")
ENDIF()
ENDIF()
