IF (NOT ${M_gnu_cxx} MATCHES "NOTFOUND")
CHECK_MUTATEE_COMPILER ("${M_gnu_cxx}"
	"${MUTATOR_DEFINES}  ${CXXFLAGS} -DSOLO_MUTATEE ${MUTATEE_CXXFLAGS_GNU}     -Wl,-export-dynamic -ldl -ldl"
	"-Wl,-export-dynamic -ldl"
	dummyMUTATEE_gxx64dyn)
IF (dummyMUTATEE_gxx64dyn)
SET (MUTATEE_gxx64dyn 1 CACHE STRING "Build mutatees: compiler g++, ABI 64-bit, dyn linked")
ELSE()
SET (MUTATEE_gxx64dyn 0 CACHE STRING "Skip mutatees: compilers g++, ABI 64-bit, dyn linked")
ENDIF()
ENDIF()
IF (NOT ${M_gnu_cxx} MATCHES "NOTFOUND")
CHECK_MUTATEE_COMPILER ("${M_gnu_cxx}"
	"${MUTATOR_DEFINES}  ${CXXFLAGS} -DSOLO_MUTATEE ${MUTATEE_CXXFLAGS_GNU}     -static -ldl -ldl"
	"-static -ldl"
	dummyMUTATEE_gxx64stat)
IF (dummyMUTATEE_gxx64stat)
SET (MUTATEE_gxx64stat 1 CACHE STRING "Build mutatees: compiler g++, ABI 64-bit, stat linked")
ELSE()
SET (MUTATEE_gxx64stat 0 CACHE STRING "Skip mutatees: compilers g++, ABI 64-bit, stat linked")
ENDIF()
ENDIF()
IF (NOT ${M_gnu_cc} MATCHES "NOTFOUND")
CHECK_MUTATEE_COMPILER ("${M_gnu_cc}"
	"${MUTATOR_DEFINES}  ${CFLAGS} -DSOLO_MUTATEE ${MUTATEE_CFLAGS_GNU}     -Wl,-export-dynamic -ldl -ldl"
	"-Wl,-export-dynamic -ldl"
	dummyMUTATEE_gcc64dyn)
IF (dummyMUTATEE_gcc64dyn)
SET (MUTATEE_gcc64dyn 1 CACHE STRING "Build mutatees: compiler gcc, ABI 64-bit, dyn linked")
ELSE()
SET (MUTATEE_gcc64dyn 0 CACHE STRING "Skip mutatees: compilers gcc, ABI 64-bit, dyn linked")
ENDIF()
ENDIF()
IF (NOT ${M_gnu_cc} MATCHES "NOTFOUND")
CHECK_MUTATEE_COMPILER ("${M_gnu_cc}"
	"${MUTATOR_DEFINES}  ${CFLAGS} -DSOLO_MUTATEE ${MUTATEE_CFLAGS_GNU}     -static -ldl -ldl"
	"-static -ldl"
	dummyMUTATEE_gcc64stat)
IF (dummyMUTATEE_gcc64stat)
SET (MUTATEE_gcc64stat 1 CACHE STRING "Build mutatees: compiler gcc, ABI 64-bit, stat linked")
ELSE()
SET (MUTATEE_gcc64stat 0 CACHE STRING "Skip mutatees: compilers gcc, ABI 64-bit, stat linked")
ENDIF()
ENDIF()
