#ifndef DYNINST_DYNCOMPAT_BIND_BIND_HPP
#define DYNINST_DYNCOMPAT_BIND_BIND_HPP

#include <functional>

namespace dyncompat {

using std::bind;
using std::ref;

namespace placeholders = std::placeholders;

} // namespace dyncompat

#endif
