
#if !defined(DYNINST_VERSION_H)

#define DYNINST_MAJOR_VERSION=${DYNINST_MAJOR_VERSION}
#define DYNINST_MINOR_VERSION=${DYNINST_MINOR_VERSION}
#define DYNINST_PATCH_VERSION=${DYNINST_PATCH_VERSION}


#define (DYNINST_VERSION_H)
#endif // DYNINST_VERSION_H
