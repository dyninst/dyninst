An \code{EventSingleStep} triggers when a thread, which was put in single-step
mode by \code{Thread::setSingleStep}, completes a single step operation.
The Thread will remain in single-step mode after completion of this event
(presuming it has not be explicitly disabled by Thread::setSingleStep).

Associated EventType Code: \code{SingleStep}
