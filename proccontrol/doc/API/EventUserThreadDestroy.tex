An \code{EventUserThreadDestroy} triggers when a thread exits. Event\'s
getThread member function returns the thread that exited.

If the platform also supports EventLWPDestroy events, this event will precede an
EventLWPDestroy event.

Associated EventType Code: \code{UserThreadDestroy}
